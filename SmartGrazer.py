class SmartGrazer(object):
    def run(self):
        # load configuration

        tries = 10000
        found = False
        while tries > 0 and not found:
            # attack

            # analyse

            # adjust

            tries = tries - 1
        pass


if __name__ == '__main__':
    smartgrazer = SmartGrazer()
    smartgrazer.run()
